node_modules/
*.log

# Generated by `npm run build` and deterministic from the sources in this repo, so they are not
# committed: tidHelperDataJson.ts is the data/*.json files re-encoded, and tidHelperHtml.ts is the
# whole page inlined into one string. Committing them would double the repository for no new
# information. Run the build to produce them and diff against what the site actually serves.
mobile/src/offline/tidHelperDataJson.ts
mobile/src/offline/tidHelperData.ts
mobile/src/offline/tidHelperHtml.ts
